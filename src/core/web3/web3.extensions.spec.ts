import { Test } from '@nestjs/testing'
import { expect } from 'chai'
import { Web3Method } from './web3.extensions'

describe('web3.extensions', () => {

  describe('Web3Method', () => {
    it('should validate args properly', () => {
      expect(true).to.equals(true)
    })
  })
})
