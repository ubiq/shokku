import { expect } from 'chai'

describe('network-chain.decorator', () => {

  describe('/', () => {
    it('should validate properly a valid /:network/:chain request', () => {
      expect(true).to.equals(true)
    })
  })
})
